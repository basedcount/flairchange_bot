use('flairChangeBot');

const aggregation = [{
    $project: {
        _id: 0,
        name: 1,
        changes: {
            $size: '$flair'
        },
        flair: 1
    }
}, {
    $sort: {
        changes: -1
    }
}, {
    $limit: 10
}];

db.PCM_users.aggregate(aggregation);

const aggr = [
    {
        $project: {
            _id: 0,
            id: 0,
            dateAdded: 0,
            optOut: 0
        }
    },
    {
        $set: {
            size: {
                $size: '$flair'
            }
        }
    },
    {
       $match: {
           size: {
               $gt: 1
           }
        }
    },
    {
        $count: 'flairChangers'
    }
]

db.PCM_users.aggregate(aggr)